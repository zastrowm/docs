---
import { Aside } from '@astrojs/starlight/components';

interface Props {
  languages: string;
}

const { languages } = Astro.props;
---

<Aside type="note" title="Language Support">
  This provider is only supported in {languages}.
</Aside>
